/**
 * @license
 * Copyright 2022-2023 Project CHIP Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { MatterDevice } from "../../MatterDevice.js";
import { AnyAttributeServer } from "../../cluster/server/AttributeServer.js";
import { CommandServer } from "../../cluster/server/CommandServer.js";
import { EventServer } from "../../cluster/server/EventServer.js";
import { Message } from "../../codec/MessageCodec.js";
import { AttributeId } from "../../datatype/AttributeId.js";
import { ClusterId } from "../../datatype/ClusterId.js";
import { CommandId } from "../../datatype/CommandId.js";
import { EndpointNumber } from "../../datatype/EndpointNumber.js";
import { EventId } from "../../datatype/EventId.js";
import { Endpoint } from "../../device/Endpoint.js";
import { MessageExchange } from "../../protocol/MessageExchange.js";
import { ProtocolHandler } from "../../protocol/ProtocolHandler.js";
import { StorageContext } from "../../storage/StorageContext.js";
import { TypeFromSchema } from "../../tlv/TlvSchema.js";
import { DataReportPayload } from "./AttributeDataEncoder.js";
import { InteractionServerMessenger, InvokeRequest, InvokeResponse, ReadRequest, SubscribeRequest, TimedRequest, WriteRequest, WriteResponse } from "./InteractionMessenger.js";
import { TlvAttributePath, TlvClusterPath, TlvCommandPath, TlvEventPath } from "./InteractionProtocol.js";
export declare const INTERACTION_PROTOCOL_ID = 1;
export declare const INTERACTION_MODEL_REVISION = 11;
export interface CommandPath {
    endpointId: EndpointNumber;
    clusterId: ClusterId;
    commandId: CommandId;
}
export interface AttributePath {
    endpointId: EndpointNumber;
    clusterId: ClusterId;
    attributeId: AttributeId;
}
export interface EventPath {
    endpointId: EndpointNumber;
    clusterId: ClusterId;
    eventId: EventId;
}
export interface AttributeWithPath {
    path: TypeFromSchema<typeof TlvAttributePath>;
    attribute: AnyAttributeServer<any>;
}
export interface EventWithPath {
    path: TypeFromSchema<typeof TlvEventPath>;
    event: EventServer<any>;
}
export interface CommandWithPath {
    path: TypeFromSchema<typeof TlvCommandPath>;
    command: CommandServer<any, any>;
}
export declare function genericElementPathToId(endpointId: EndpointNumber | undefined, clusterId: ClusterId | undefined, elementId: number | undefined): string;
export declare function commandPathToId({ endpointId, clusterId, commandId }: CommandPath): string;
export declare function attributePathToId({ endpointId, clusterId, attributeId }: TypeFromSchema<typeof TlvAttributePath>): string;
export declare function eventPathToId({ endpointId, clusterId, eventId }: TypeFromSchema<typeof TlvEventPath>): string;
export declare function clusterPathToId({ nodeId, endpointId, clusterId }: TypeFromSchema<typeof TlvClusterPath>): string;
export type InteractionServerOptions = {
    subscriptionMaxIntervalSeconds?: number;
    subscriptionMinIntervalSeconds?: number;
    subscriptionRandomizationWindowSeconds?: number;
};
export declare class InteractionServer implements ProtocolHandler<MatterDevice> {
    private readonly storage;
    private readonly options?;
    private endpointStructure;
    private nextSubscriptionId;
    private eventHandler;
    private readonly subscriptionMap;
    private isClosing;
    constructor(storage: StorageContext, options?: InteractionServerOptions | undefined);
    getId(): number;
    setRootEndpoint(endpoint: Endpoint): void;
    onNewExchange(exchange: MessageExchange<MatterDevice>): Promise<void>;
    handleReadRequest(exchange: MessageExchange<MatterDevice>, { attributeRequests, dataVersionFilters, eventRequests, eventFilters, isFabricFiltered, interactionModelRevision, }: ReadRequest): DataReportPayload;
    handleWriteRequest(exchange: MessageExchange<MatterDevice>, { suppressResponse, timedRequest, writeRequests, interactionModelRevision }: WriteRequest, { packetHeader: { sessionType } }: Message): WriteResponse;
    handleSubscribeRequest(exchange: MessageExchange<MatterDevice>, { minIntervalFloorSeconds, maxIntervalCeilingSeconds, attributeRequests, dataVersionFilters, eventRequests, eventFilters, keepSubscriptions, isFabricFiltered, interactionModelRevision, }: SubscribeRequest, messenger: InteractionServerMessenger): Promise<void>;
    handleInvokeRequest(exchange: MessageExchange<MatterDevice>, { invokeRequests, timedRequest, suppressResponse, interactionModelRevision }: InvokeRequest, message: Message): Promise<InvokeResponse>;
    handleTimedRequest(exchange: MessageExchange<MatterDevice>, { timeout, interactionModelRevision }: TimedRequest): void;
    close(): Promise<void>;
}
//# sourceMappingURL=InteractionServer.d.ts.map