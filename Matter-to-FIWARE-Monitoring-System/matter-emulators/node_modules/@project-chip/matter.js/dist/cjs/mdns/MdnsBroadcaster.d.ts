/**
 * @license
 * Copyright 2022-2023 Project CHIP Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { CommissionerInstanceData, CommissioningModeInstanceData, OperationalInstanceData, PairingHintBitmap } from "../common/InstanceBroadcaster.js";
import { Fabric } from "../fabric/Fabric.js";
import { TypeFromPartialBitSchema } from "../schema/BitmapSchema.js";
import { MdnsServer } from "./MdnsServer.js";
/**
 * This class is handing MDNS Announcements for multiple instances/devices
 */
export declare class MdnsBroadcaster {
    private readonly mdnsServer;
    private readonly enableIpv4?;
    static create(options?: {
        enableIpv4?: boolean;
        multicastInterface?: string;
    }): Promise<MdnsBroadcaster>;
    private readonly network;
    private readonly activeCommissioningAnnouncements;
    private readonly activeOperationalAnnouncements;
    constructor(mdnsServer: MdnsServer, enableIpv4?: boolean | undefined);
    validatePairingInstructions(pairingHint: TypeFromPartialBitSchema<typeof PairingHintBitmap>, pairingInstructions: string): void;
    private getIpRecords;
    /** Set the Broadcaster data to announce a device ready for commissioning in a special mode */
    setCommissionMode(announcedNetPort: number, mode: number, { deviceName, deviceType, vendorId, productId, discriminator, sessionIdleInterval, sessionActiveInterval, sessionActiveThreshold, pairingHint, pairingInstructions, }: CommissioningModeInstanceData): Promise<void>;
    /** Set the Broadcaster Data to announce a device for operative discovery (aka "already paired") */
    setFabrics(announcedNetPort: number, fabrics: Fabric[], { sessionIdleInterval, sessionActiveInterval, sessionActiveThreshold, }?: OperationalInstanceData): Promise<void>;
    /** Set the Broadcaster data to announce a Commissioner (aka Commissioner discovery) */
    setCommissionerInfo(announcedNetPort: number, { deviceName, deviceType, vendorId, productId, sessionIdleInterval, sessionActiveInterval, sessionActiveThreshold, }: CommissionerInstanceData): Promise<void>;
    announce(announcementPort: number): Promise<void>;
    expireFabricAnnouncement(announcementPort: number): Promise<void>;
    expireCommissioningAnnouncement(announcementPort: number): Promise<void>;
    expireAllAnnouncements(announcementPort: number): Promise<void>;
    close(): Promise<void>;
}
//# sourceMappingURL=MdnsBroadcaster.d.ts.map