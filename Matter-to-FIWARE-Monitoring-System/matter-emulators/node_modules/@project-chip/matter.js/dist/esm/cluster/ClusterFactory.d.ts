/**
 * @license
 * Copyright 2022-2023 Project CHIP Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { MatterError } from "../common/MatterError.js";
import { ClusterId } from "../datatype/ClusterId.js";
import { BitSchema, TypeFromPartialBitSchema } from "../schema/BitmapSchema.js";
import { Branded, Merge } from "../util/Type.js";
import { Attribute as ClusterAttribute, Command as ClusterCommand, Event as ClusterEvent, ConditionalFeatureList, GlobalAttributes } from "./Cluster.js";
export declare class IllegalClusterError extends MatterError {
}
/**
 * The formal definitions of clusters in matter.js are generated
 * programmatically.  There is no single formal "cluster factory" but rather a
 * set of types and methods for each cluster.
 *
 * Individual modules associated with each cluster use the utility types and
 * methods as building blocks.
 *
 * Note:  There is overlap between the types here and those in Cluster.ts.
 * They may converge over time but are fully compatible.
 */
export declare namespace ClusterFactory {
    /**
     * An "element set" defines the set of elements (commands, attributes or
     * events) of a cluster.
     */
    type ElementSet<T> = Record<string, T | undefined>;
    /**
     * Definition of a cluster attribute.
     */
    type Attribute = ClusterAttribute<any, any>;
    /**
     * Definition of a cluster command.
     */
    type Command = ClusterCommand<any, any, any>;
    /**
     * Definition of a cluster event.
     */
    type Event = ClusterEvent<any, any>;
    /**
     * These fields uniquely identify a cluster.
     */
    interface Identity {
        readonly id: ClusterId;
        readonly name: string;
        readonly revision: number;
    }
    /**
     * Cluster "elements" are attributes, commands and events that may comprise
     * a cluster.
     */
    interface ClusterElements {
        readonly attributes: ElementSet<Attribute>;
        readonly commands: ElementSet<Command>;
        readonly events: ElementSet<Event>;
    }
    /**
     * A cluster "component" is a set of elements intended to be added to a
     * cluster definition.
     */
    interface Component extends ClusterElements {
    }
    /**
     * A "typed component" is a component with detailed type information.
     */
    interface TypedComponent<T extends Partial<Component>> {
        attributes: T["attributes"] extends ElementSet<Attribute> ? T["attributes"] : {};
        commands: T["commands"] extends ElementSet<Command> ? T["commands"] : {};
        events: T["events"] extends ElementSet<Event> ? T["events"] : {};
    }
    /**
     * Cluster "features" describe the features supported by a cluster.
     */
    interface Features<F extends BitSchema = {}> {
        readonly features: F;
        readonly supportedFeatures: TypeFromPartialBitSchema<F>;
        readonly unknown?: boolean;
    }
    /**
     * A Cluster represents a fully formed cluster with features selected.
     */
    interface Cluster extends Identity, Features<BitSchema>, ClusterElements {
    }
    /**
     * A "partial definition" is a cluster definition that does not require
     * empty elements to be present.
     */
    type PartialDefinition = {
        id: number;
    } & Omit<Identity, "id"> & Partial<Features> & {
        attributes?: ElementSet<Attribute>;
        commands?: ElementSet<Command>;
        events?: ElementSet<Event>;
    };
    /**
     * A "definition" is the fully typed Cluster for a PartialDefinition.
     */
    type Definition<T extends PartialDefinition> = {
        id: Branded<T["id"], "ClusterId">;
        name: T["name"];
        revision: T["revision"];
        features: T["features"] extends {} ? T["features"] : {};
        supportedFeatures: T["supportedFeatures"] extends {} ? T["supportedFeatures"] : {};
        attributes: T["attributes"] extends infer A extends {} ? Merge<A, GlobalAttributes<T["features"] extends {} ? T["features"] : {}>> : {};
        commands: T["commands"] extends {} ? T["commands"] : {};
        events: T["events"] extends {} ? T["events"] : {};
        unknown: T["unknown"] extends boolean ? T["unknown"] : false;
    } & Omit<T, "attributes">;
    /**
     * A "MutableDefinition" is a Cluster with fields that may be modified.
     */
    type MutableDefinition = {
        -readonly [Key in keyof Cluster]: Cluster[Key];
    };
    /**
     * An "extender" is a function that creates a cluster with specific
     * features enabled.
     */
    type Extender = (...features: any) => Cluster;
    /**
     * An "extensible" cluster is a base cluster with support for enabling
     * features.
     */
    interface Extensible extends Cluster {
        with: Extender;
    }
    /**
     * An "element" is a single attribute, command or event of a cluster.
     */
    type Element = Attribute | Command | Event;
    /**
     * Define a cluster component.
     */
    function Component<const T extends Partial<Component>>(component: T): TypedComponent<T>;
    /**
     * Define a cluster.
     */
    function Definition<const T extends PartialDefinition>(definition: T): Definition<T>;
    /**
     * Define an extensible cluster.
     */
    function Extensible<const DefinitionT extends Identity & Partial<ClusterElements> & Partial<Features>, const ExtenderT extends Extender>(definition: DefinitionT, extender: ExtenderT): {
        id: Branded<DefinitionT["id"], "ClusterId">;
        name: DefinitionT["name"];
        revision: DefinitionT["revision"];
        features: DefinitionT["features"] extends {} ? DefinitionT["features"] : {};
        supportedFeatures: DefinitionT["supportedFeatures"] extends {} ? DefinitionT["supportedFeatures"] : {};
        attributes: DefinitionT["attributes"] extends infer A extends {} ? Merge<A, GlobalAttributes<DefinitionT["features"] extends {} ? DefinitionT["features"] : {}>> : {};
        commands: DefinitionT["commands"] extends {} ? DefinitionT["commands"] : {};
        events: DefinitionT["events"] extends {} ? DefinitionT["events"] : {};
        unknown: DefinitionT["unknown"] extends boolean ? DefinitionT["unknown"] : false;
    } & Omit<DefinitionT, "attributes"> & {
        readonly with: ExtenderT;
    };
    /**
     * Define a cluster that can only be extended.
     */
    function ExtensibleOnly<const ExtenderT extends Extender>(extender: ExtenderT): {
        readonly with: ExtenderT;
    };
    /**
     * Injects a set of functionality into a cluster if the cluster supports
     * the specified features.
     *
     * This is used by extenders and does not convey type information.
     */
    function extend(definition: MutableDefinition, elements: ClusterElements, ...applicableFeatures: TypeFromPartialBitSchema<any>[]): void;
    /**
     * Validates a set of feature flags against the features supported by a
     * cluster.
     *
     * Used by extenders.
     */
    function validateFeatureSelection(features: string[], validFeatures: Record<string, string>): void;
    /**
     * Throws an error if a feature combination is illegal per the Matter
     * specification.
     *
     * Used by extenders.
     */
    function prevent(definition: Cluster, ...illegalFeatureCombinations: Record<string, boolean>[]): void;
    /**
     * Create a conditional version of an unconditional element definition.
     */
    function AsConditional<const ClusterT extends Cluster, const E extends Element, const OI extends ConditionalFeatureList<ClusterT["features"]>, const MI extends ConditionalFeatureList<ClusterT["features"]>>(element: E, { optionalIf, mandatoryIf }: {
        optionalIf?: OI;
        mandatoryIf?: MI;
    }): E & {
        readonly optional: true;
        readonly isConditional: true;
        readonly optionalIf: [] | OI;
        readonly mandatoryIf: [] | MI;
    };
    /**
     * Extract the type of a cluster's attributes.
     */
    type AttributesOf<C extends Cluster> = C extends {
        attributes: infer E extends {
            [K in string]: Attribute;
        };
    } ? {
        [K in Extract<keyof E, string> as E[K] extends Attribute ? K : never]: E[K];
    } : never;
    /**
     * Extract the type of a cluster's commands.
     */
    type CommandsOf<C extends Cluster> = C extends {
        commands: infer E extends {
            [K in string]: Command;
        };
    } ? {
        [K in Extract<keyof E, string> as E[K] extends Command ? K : never]: E[K];
    } : never;
    /**
     * Extract the type of a cluster's events.
     */
    type EventsOf<C extends Cluster> = C extends {
        events: infer E extends {
            [K in string]: Event;
        };
    } ? {
        [K in Extract<keyof E, string> as E[K] extends Event ? K : never]: E[K];
    } : never;
}
//# sourceMappingURL=ClusterFactory.d.ts.map