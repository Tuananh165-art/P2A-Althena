"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var conformance_exports = {};
__export(conformance_exports, {
  addConformance: () => addConformance
});
module.exports = __toCommonJS(conformance_exports);
var import_Logger = require("../../../log/Logger.js");
var import_String = require("../../../util/String.js");
var import_aspects = require("../../aspects/index.js");
/**
 * @license
 * Copyright 2022-2023 Project CHIP Authors
 * SPDX-License-Identifier: Apache-2.0
 */
const logger = import_Logger.Logger.get("conformance-validation");
const BinaryOperatorMap = {
  [import_aspects.Conformance.Operator.AND]: "&&",
  [import_aspects.Conformance.Operator.EQ]: "===",
  [import_aspects.Conformance.Operator.NE]: "!==",
  [import_aspects.Conformance.Operator.OR]: "||",
  [import_aspects.Conformance.Operator.GT]: ">",
  [import_aspects.Conformance.Operator.LT]: "<",
  [import_aspects.Conformance.Operator.GTE]: ">=",
  [import_aspects.Conformance.Operator.LTE]: "<="
};
function addConformance(builder, model, conformance) {
  builder.addTest(
    `(${expr(conformance.ast).text})`,
    "CONFORMANCE_VIOLATION",
    model,
    `Value of ${(0, import_String.camelize)(model.name, false)} is disallowed by conformance`
  );
  function expr(ast) {
    if (!ast) {
      logger.error("Undefined conformance AST type");
      return { text: "true" };
    }
    switch (ast?.type) {
      case void 0:
        logger.error("Missing conformance AST type");
        return { text: "true" };
      default:
        logger.error(`Unknown conformance AST type ${ast.type}`);
        return { text: "true" };
      case import_aspects.Conformance.Special.Empty:
      case import_aspects.Conformance.Special.Desc:
      case import_aspects.Conformance.Flag.Provisional:
      case import_aspects.Conformance.Flag.Deprecated:
      case import_aspects.Conformance.Flag.Optional:
        return { text: "true" };
      case import_aspects.Conformance.Special.Choice:
        builder.hasChoices = true;
        const choice = ast.param;
        const choiceName = `${model.parent?.path || "?"}.${choice.name}`;
        return {
          text: `this.testChoice(${JSON.stringify(choiceName)}, ${expr(choice.expr).text}, (v !== undefined && v !== null), ${choice.num}, ${choice.orMore ? "true" : "false"})`
        };
      case import_aspects.Conformance.Special.Group:
        if (!Array.isArray(ast.param)) {
          logger.error("Conformance AST group parameter is not an array");
          return { text: "true" };
        }
        return { text: `this.testGroup(${ast.param.map((e) => expr(e).text).join(", ")})` };
      case import_aspects.Conformance.Special.Name:
        const name = ast.param;
        if (builder.definedFeatures.has(name)) {
          if (builder.enabledFeatures.has(name)) {
            return { text: "(v !== undefined && v !== null)", feature: true };
          }
          return { text: "(v === undefined || v === null)", feature: false };
        }
        return { text: `record[${JSON.stringify(name)}]` };
      case import_aspects.Conformance.Special.OptionalIf: {
        const e = expr(ast.param);
        if (e.feature === true) {
          return { text: "true" };
        }
        if (e.feature === false) {
          return { text: "(v === undefined || v === null)" };
        }
        return { text: `(${e.text} || v === undefined || v === null)` };
      }
      case import_aspects.Conformance.Special.Value:
        return { text: `(record[${JSON.stringify(ast.param)}] == undefined ? true : false)` };
      case import_aspects.Conformance.Flag.Disallowed:
        return { text: "false" };
      case import_aspects.Conformance.Flag.Mandatory:
        return { text: "(v !== undefined && v !== null)" };
      case import_aspects.Conformance.Operator.AND:
      case import_aspects.Conformance.Operator.EQ:
      case import_aspects.Conformance.Operator.NE:
      case import_aspects.Conformance.Operator.OR:
      case import_aspects.Conformance.Operator.GT:
      case import_aspects.Conformance.Operator.LT:
      case import_aspects.Conformance.Operator.GTE:
      case import_aspects.Conformance.Operator.LTE:
      case import_aspects.Conformance.Operator.XOR: {
        const e1 = expr(ast.param.lhs);
        const e2 = expr(ast.param.rhs);
        if (e1.feature !== void 0 && e2.feature !== void 0) {
          let feature;
          switch (ast.type) {
            case import_aspects.Conformance.Operator.AND:
              feature = e1.feature && e2.feature;
              break;
            case import_aspects.Conformance.Operator.OR:
              feature = e1.feature || e2.feature;
              break;
            case import_aspects.Conformance.Operator.XOR:
              feature = e1.feature !== e2.feature;
              break;
          }
          if (feature !== void 0) {
            return {
              text: feature ? "(v !== undefined && v !== null)" : "(v === undefined || v === null)",
              feature
            };
          }
        }
        if (ast.type == import_aspects.Conformance.Operator.XOR) {
          return { text: `${e1.text} ? !${e2.text} : ${e2.text}` };
        }
        return { text: `${e1.text} ${BinaryOperatorMap[ast.type]} ${e2.text}` };
      }
      case import_aspects.Conformance.Operator.NOT: {
        const e = expr(ast.param);
        if (e.feature !== void 0) {
          e.feature = !e.feature;
        }
        return { text: `!${e.text}`, feature: e.feature };
      }
    }
  }
}
//# sourceMappingURL=conformance.js.map
