import { MatterDevice } from "../MatterDevice.js";
import { NodeId } from "../datatype/NodeId.js";
import { VendorId } from "../datatype/VendorId.js";
import { Endpoint } from "../device/Endpoint.js";
import { Fabric, FabricBuilder } from "../fabric/Fabric.js";
import { Timer } from "../time/Time.js";
import { ByteArray } from "../util/ByteArray.js";
import { MatterFlowError } from "./MatterError.js";
export declare class MatterFabricConflictError extends MatterFlowError {
}
/** Class to Handle one FailSafe context. This is mainly used when adding (Commissioning) or updating new Fabrics. */
export declare class FailSafeManager {
    private readonly device;
    associatedFabric: Fabric | undefined;
    private readonly expiryCallback;
    readonly rootEndpoint: Endpoint;
    readonly fabricBuilder: FabricBuilder;
    failSafeTimer: Timer;
    private maxCumulativeFailsafeTimer;
    private storedNetworkClusterState;
    csrSessionId: number | undefined;
    forUpdateNoc: boolean | undefined;
    constructor(device: MatterDevice, associatedFabric: Fabric | undefined, expiryLengthSeconds: number, maxCumulativeFailsafeSeconds: number, expiryCallback: () => Promise<void>, rootEndpoint: Endpoint);
    /** Store required CLuster data when opening the FailSafe context to allow to restore them on expiry. */
    private storeEndpointState;
    /** Restore Cluster data when the FailSafe context expired. */
    restoreEndpointState(endpoint?: Endpoint): void;
    get fabricIndex(): import("../datatype/FabricIndex.js").FabricIndex | undefined;
    /** Handle "Re-Arming" an existing FailSafe context to extend the timer, expire or fail if not allowed. */
    reArm(associatedFabric: Fabric | undefined, expiryLengthSeconds: number): Promise<void>;
    /** Expire the FailSafe context. This is called by the timer and can also be called manually if needed. */
    expire(): Promise<void>;
    /** Complete the FailSafe context. This is called when the commissioning is completed. */
    complete(): void;
    /**
     * Handles a CSR from OperationalCredentials cluster and stores additional internal information for further
     * validity checks.
     */
    createCertificateSigningRequest(isForUpdateNoc: boolean, sessionId: number): Uint8Array;
    /** Handles adding a trusted root certificate from Operational Credentials cluster. */
    setRootCert(rootCert: ByteArray): void;
    /**
     * Build a new Fabric object based on an existing fabric for the "UpdateNoc" case of the Operational Credentials
     * cluster.
     */
    buildUpdatedFabric(nocValue: ByteArray, icacValue: ByteArray | undefined): Promise<Fabric>;
    /** Build a new Fabric object for a new fabric for the "AddNoc" case of the Operational Credentials cluster. */
    buildFabric(nocData: {
        nocValue: ByteArray;
        icacValue: ByteArray | undefined;
        adminVendorId: VendorId;
        ipkValue: ByteArray;
        caseAdminSubject: NodeId;
    }): Promise<Fabric>;
}
//# sourceMappingURL=FailSafeManager.d.ts.map