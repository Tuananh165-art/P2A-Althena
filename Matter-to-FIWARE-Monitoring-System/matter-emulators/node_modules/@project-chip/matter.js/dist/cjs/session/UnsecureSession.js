"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var UnsecureSession_exports = {};
__export(UnsecureSession_exports, {
  UnsecureSession: () => UnsecureSession
});
module.exports = __toCommonJS(UnsecureSession_exports);
var import_MessageCodec = require("../codec/MessageCodec.js");
var import_MatterError = require("../common/MatterError.js");
var import_NodeId = require("../datatype/NodeId.js");
var import_MessageReceptionState = require("../protocol/MessageReceptionState.js");
var import_SecureSession = require("./SecureSession.js");
var import_Session = require("./Session.js");
var import_SessionManager = require("./SessionManager.js");
/**
 * @license
 * Copyright 2022-2023 Project CHIP Authors
 * SPDX-License-Identifier: Apache-2.0
 */
class UnsecureSession extends import_Session.Session {
  constructor(args) {
    const { context, initiatorNodeId, isInitiator } = args;
    super({
      ...args,
      setActiveTimestamp: !isInitiator,
      // When we are the initiator we assume the node is in idle mode
      messageReceptionState: new import_MessageReceptionState.MessageReceptionStateUnencryptedWithRollover()
    });
    this.closingAfterExchangeFinished = false;
    this.context = context;
    this.initiatorNodeId = initiatorNodeId ?? import_NodeId.NodeId.getRandomOperationalNodeId();
  }
  isSecure() {
    return false;
  }
  isPase() {
    return false;
  }
  decode(packet) {
    return import_MessageCodec.MessageCodec.decodePayload(packet);
  }
  encode(message) {
    return import_MessageCodec.MessageCodec.encodePayload(message);
  }
  getAttestationChallengeKey() {
    throw new import_MatterError.MatterFlowError("Not supported on an unsecure session");
  }
  setFabric(_fabric) {
    throw new import_MatterError.MatterFlowError("Not supported on an unsecure session");
  }
  get name() {
    return `unsecure/${this.initiatorNodeId}`;
  }
  getContext() {
    return this.context;
  }
  getId() {
    return import_SessionManager.UNICAST_UNSECURE_SESSION_ID;
  }
  getPeerSessionId() {
    return import_SessionManager.UNICAST_UNSECURE_SESSION_ID;
  }
  getNodeId() {
    return this.initiatorNodeId;
  }
  getPeerNodeId() {
    return void 0;
  }
  async destroy() {
    await this.end();
  }
  async end() {
    await this.closeCallback?.();
  }
  getAssociatedFabric() {
    throw new import_SecureSession.NoAssociatedFabricError("Session needs to be a secure session");
  }
}
//# sourceMappingURL=UnsecureSession.js.map
