/**
 * @license
 * Copyright 2022-2023 Project CHIP Authors
 * SPDX-License-Identifier: Apache-2.0
 */
import { MatterDevice } from "../../MatterDevice.js";
import { Message } from "../../codec/MessageCodec.js";
import { CommandId } from "../../datatype/CommandId.js";
import { Endpoint } from "../../device/Endpoint.js";
import { StatusCode } from "../../protocol/interaction/StatusCode.js";
import { Session } from "../../session/Session.js";
import { TlvSchema, TlvStream } from "../../tlv/TlvSchema.js";
export declare class CommandServer<RequestT, ResponseT> {
    readonly invokeId: CommandId;
    readonly responseId: CommandId;
    readonly name: string;
    readonly requestSchema: TlvSchema<RequestT>;
    readonly responseSchema: TlvSchema<ResponseT>;
    readonly requiresTimedInteraction: boolean;
    protected readonly handler: (request: RequestT, session: Session<MatterDevice>, message: Message, endpoint: Endpoint) => Promise<ResponseT> | ResponseT;
    constructor(invokeId: CommandId, responseId: CommandId, name: string, requestSchema: TlvSchema<RequestT>, responseSchema: TlvSchema<ResponseT>, requiresTimedInteraction: boolean, handler: (request: RequestT, session: Session<MatterDevice>, message: Message, endpoint: Endpoint) => Promise<ResponseT> | ResponseT);
    invoke(session: Session<MatterDevice>, args: TlvStream, message: Message, endpoint: Endpoint): Promise<{
        /** Primary StatusCode of the invoke request  as defined by Interaction proptocol. */
        code: StatusCode;
        /** Cluster specific StatusCode of the invoke request if required */
        clusterCode?: number;
        /** ID of the response */
        responseId: CommandId;
        /** Response data */
        response: TlvStream;
    }>;
}
//# sourceMappingURL=CommandServer.d.ts.map