/**
 * Cache computed values or resources for a specified duration to improve performances.
 *
 * @license
 * Copyright 2022-2023 Project CHIP Authors
 * SPDX-License-Identifier: Apache-2.0
 */
export declare class Cache<T> {
    private readonly generator;
    private readonly expirationMs;
    private readonly expireCallback?;
    private readonly knownKeys;
    private readonly values;
    private readonly timestamps;
    private readonly periodicTimer;
    constructor(generator: (...params: any[]) => T, expirationMs: number, expireCallback?: ((key: string, value: T) => Promise<void>) | undefined);
    get(...params: any[]): T;
    keys(): string[];
    private deleteEntry;
    clear(): Promise<void>;
    close(): Promise<void>;
    private expire;
}
//# sourceMappingURL=Cache.d.ts.map